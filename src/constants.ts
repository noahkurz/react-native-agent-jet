/** Values shared by the in-app bridge and the host, so the two ends cannot drift apart. */

export const DEFAULT_PORT = 8765;

/** The Android emulator reaches the host's loopback interface through this alias. */
export const ANDROID_EMULATOR_HOST = "10.0.2.2";

export const LOCALHOST = "localhost";
